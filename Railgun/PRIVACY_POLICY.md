# 隐私说明

Railgun 自动签到助手只在本机 Chrome 浏览器内运行。

- 扩展只访问 `https://railgun.info/*`。
- 可选 Cookie 仅保存到本机 Chrome 扩展存储，用于在 Railgun 域名下恢复登录态。
- 启用 Gmail 自动登录时，扩展会读取 `https://mail.google.com/mail/feed/atom`，并仅用于搜索和解析最近的 Railgun 验证码邮件。
- 签到日志仅保存在本机扩展存储，最多保留最近 30 条。
- 扩展不收集、不上传、不分析任何个人数据。
